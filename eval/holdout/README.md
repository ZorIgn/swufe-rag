# Holdout

Holdout questions are supplied with the released dataset rather than embedded in
production code. `eval/run_generalization.py` selects database entities at runtime
and compares typed tool output to direct repository truth.
